export default function UniformLayerContainer(props) {
    return (
        <div>
            <p>Layer Container Width: {String(props.width)}</p>
            <p>Layer Container Height: {String(props.height)}</p>
        </div>
    )
}